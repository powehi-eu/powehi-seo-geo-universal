#!/usr/bin/env bash
set -euo pipefail

echo "Warning: 'claude-seo' is deprecated; use 'powehi-seo-geo'." >&2
SCRIPT_DIR="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
exec "${SCRIPT_DIR}/powehi-seo-geo" "$@"
